[package]
name = "my_cool_package"
authors = ["my@joinme.com"]
compiler_version = ">=0.18.0"
version = "0.1.1"
type = "lib"
description = "This is a test package! Let's go!"
keywords = ["utility", "tool"]

[dependencies]
aztec = { path = "../aztec" }
value_note = { path = "../value-note" }